# Variables are declared with the `var` keyword
fn main():
    try:
        do_math(23)
        add_one(23)
    except:
        print("Error")

def do_math(x):
    var y = x + x
    y = y * y
    print(y)

def add_one(x):
    var y: Int = 1
    print(x + y)